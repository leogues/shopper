export const promptText = {
  extractMeterValues:
    'In the image, extract only the numerical values directly associated with the meter reading. The response should include only the numeric value and nothing else.',
}
